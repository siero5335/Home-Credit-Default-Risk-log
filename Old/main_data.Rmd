---
title: "main_data"
author: '@siero5335'
date: "2018/5/18"
output: html_document
---

```{r setup, include=FALSE}
if (!require("pacman")) install.packages("pacman")
pacman::p_load(tidyverse, skimr, GGally, plotly, viridis, caret, DT, data.table, lightGBM)
```

## Data loading
```{r}
train <-fread('application_train.csv', stringsAsFactors = FALSE, showProgress=F,
              data.table = F, na.strings=c("NA","NaN","?", ""))

test <-fread('application_test.csv', stringsAsFactors = FALSE, showProgress=F,
             data.table = F, na.strings=c("NA","NaN","?", ""))
```


目的の方はCURRIDに一本化されるので、PrevIDをまとめた特徴量作らないとダメ

## Data status
```{r}
str(train)
```

```{r}
str(test)
```