---
title: "previous_application_EDA"
author: '@siero5335'
date: "2018/5/18"
output: html_document
---

```{r setup, include=FALSE}
if (!require("pacman")) install.packages("pacman")
pacman::p_load(tidyverse, skimr, GGally, plotly, viridis, DT, data.table)
options(scipen=5)
```

## Data loading
```{r}
Previous <-fread('previous_application.csv', stringsAsFactors = FALSE, showProgress=F,
                     data.table = F, na.strings=c("NA","NaN","?", ""))
```

## Data status
```{r}
str(Pos_cash)
```

```{r}
str(Previous)
```

## Join data
```{r}
inst_credit <- dplyr::inner_join(Previous, Pos_cash, by=c("SK_ID_PREV", "SK_ID_CURR"))
rm(installments, credit)
gc()
#fwrite(bureau_join, "bureau_join.csv")
```




















